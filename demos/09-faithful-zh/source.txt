作者在这一章提出了一个评估检索增强生成（RAG）系统的三阶段框架。第一阶段
是检索质量，第二阶段是有据性，第三阶段是端到端可用性。

第一阶段检索质量的主要指标是召回率 @k 和平均倒数排名，需要在一个有标注
的评测集上计算。

第二阶段有据性，指标是生成的句子中有多少比例可以追溯到检索出的段落。

第三阶段端到端可用性，指标是下游任务的成功率。

作者强调，只汇报第一阶段指标的基准测试是有误导性的。检索召回率高不等于
有据性高；有据性高也不等于下游任务成功率高。
